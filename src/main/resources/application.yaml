spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mediclam_db
    username: root
    password: 1999

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true